<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="stylesheet" href="./css/styles.css">
    <title>
        View Reports
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .flex-container {
            display: flex;
            flex-wrap: wrap;
            flex-direction: column;
            /* background-color:	#00ffff; */
        }
        .flex-container>div {
            display: inline-block;
            background-color: 	#ffffe0;
            margin: 10px;
            text-align: center;
            line-height: 20px;
            font-size: 25px;
        }
    </style>
</head>

<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
                <b class="text-white bg-blue-600 hover:bg-blue-800 font-medium rounded px-3 py-1 "><a href="/viewReports">Go
                        Back</a></b>
                <b class="text-white bg-blue-600 hover:bg-blue-800 font-medium rounded px-3 py-1 "><a
                        href="/signout">Signout</a></b>
                <h2  style="color: blue" class="mt-3"><b>Reports:</b></h2>
                <p>Total number of sessions organized are <b><%=NoOfSess%></b> from <b><%=(new Date(Date1)).toDateString()%>, <%=(new Date(Date1)).toLocaleTimeString()%></b> to <b><%=(new Date(Date2)).toDateString()%>, <%=(new Date(Date2)).toLocaleTimeString()%></b></p>
                <h2  style="color: blue" class="mt-3"><b>Number of Sport Sessions:</b></h2>
                <div class="flex-container">
                    <%if(reports.length!=0){%>
                        <% for(var i=0; i< reports.length; i++) { %>
                            <div class="font-medium rounded px-2 py-1">
                                <%=reports[i].sportName%>: <%=reports[i].count%> <%if(i===0){%><p style="display: inline;" class="rounded">(Most Popular)</p><%}%>
                            </div>
                            <%}%>
                    <%}else{%>
                        <p>No Sport Sessions were organized</p>
                    <%}%>
                </div>
            </div>
        </div>
    </div>

</body>

</html>
